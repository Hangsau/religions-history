# 實作清單：翻譯 checkpoint、吞吐量量測與心理學標籤對接

> 來源：/plan-check（2026-07-20，本檔自足、可跨 session 接力）
> 工作目錄：`C:\claudehome\projects\religions-history`
> 接手須知：先讀 `CLAUDE.md`、`HANDOFF.md`、`tools/m3-translator-role.md`、`tools/m3-tagger-role.md`；不得修改 `raw/original.txt` 或 checksums；M3 同一時間只准一條生成管線。
> 背景：歷史管線有 30 部核心翻譯含 733 個 `CHUNK ... FAILED`；舊完成判定曾把 placeholder 當成 done。完成判定、tag guard、verify guard、auto/supervisor/watcher 單實例防護已在目前工作樹修補，尚待整理提交。

## 目標

1. 長文某一 chunk 失敗時，已成功 chunks 可以續用，不再從 chunk 1 重跑。
2. 正式 `01-translation.md` 仍採全成品原子寫入，不發布半成品。
3. 建立可信的每部／每 chunk 耗時與重試統計，取代 commit 數推估速度。
4. religions-history 新增 `psych_tags`，以既有 45 個人生問題細群＋3 個跨界支流對接心理學讀經入口。
5. `semantic_tags + psych_tags + keywords` 在同一次分類任務輸出，但保持三個欄位與兩套 taxonomy 分離。

## 明確不做

- 不更換 MiniMax-M3，不新增付費後端。
- 不平行啟動多條 M3 翻譯管線。
- 第一階段不調高目前 chunk 大小；先消除重跑浪費並量測。
- 不把 `semantic_tags`、`psych_tags`、psychology-schools `concept_tags` 合成單一詞彙表。
- 不重寫既有合格翻譯，不刪歷史 FAILED 檔；完整重跑成功後才由正式輸出覆寫。
- 不在本輪做章節級網站索引、註釋修復或翻譯學術校訂。

## 待執行

- [x] **Step 0 — 凍結與基線快照**（2026-07-20：HALT 後 watcher 09:06、supervisor 09:04 均退出；基線 257/518、31 failed）
  - 建立 `logs/pipeline-HALT.flag`，確認 watcher、supervisor、auto-pipeline 均退出後才編輯執行中腳本。
  - 保存 `git status --short --branch`、`pipeline-runtime.json`、FAILED marker 清單與 `pipeline-failed.json`。
  - 記錄基線：核心完整翻譯數、完整 tag 數、FAILED 檔數、最近一輪成功呼叫／timeout／有效完成數。
  - 不清理使用者或既有管線工作樹。

- [x] **Step 1 — Checkpoint 資料格式與失效規則**（2026-07-20：schema v1 + per-input/output hash + stale archive）
  - 工作目錄：`logs/pipeline-checkpoints/<slug>/translate/`（持久 runtime 範圍，明列 `.gitignore`）。
  - `manifest.json` 最少包含：slug、task、source SHA-256、role SHA-256、chunking version、chunk 大小、chunks_total、完成 chunk 編號、模型、建立／更新時間。
  - 每段寫 `chunk-0001.md`；先寫同目錄暫存檔，再以 `Path.replace()` 原子換入。
  - checkpoint 只有在 source hash、role hash、chunking version、chunks_total 全部相符時可續用；任一不符就移到同 slug 的 `stale-<timestamp>/`，不可靜默混用。
  - 正式 `01-translation.md` 只在全部 chunks 齊全、每段通過基本格式檢查後一次組裝寫入。

- [x] **Step 2 — 最小實作 translation resume**（2026-07-20：成功段原子保存、缺段續跑、全齊才發布）
  - 在 `scripts/translate.py` 的 chunked translate 路徑加入：載入 manifest → 跳過已完成 chunk → 成功一段立即 checkpoint → 失敗時保留 checkpoint 並回傳 False。
  - 組裝前檢查：chunk 數齊全、無 FAILED marker、第一段有正式 `# ... — 翻譯` header、後續段至少含預期結構內容。
  - 組裝成功後才由 `auto-pipeline.py` 寫 `translation_status=done`；checkpoint 可刪除或標 completed，選擇其一並寫入文件。
  - 直接單檔 `translate.py --slug ... --skip-done` 與 orchestrator 必須走同一套規則。

- [x] **Step 3 — 最小自動測試**（2026-07-20：stdlib unittest 13/13 PASS）
  - 使用 Python stdlib `unittest`，不新增 pytest 依賴。
  - 覆蓋：完整／短檔／FAILED placeholder 判定；成功 1–4、第五段失敗後只重試第五段；source/role hash 改變會使 checkpoint 失效；未齊全不得寫正式檔／不得標 done／不得 tag；全部完成才原子組裝。
  - 覆蓋 auto-pipeline、supervisor、watcher 單實例鎖的 stale PID 與 live PID 行為。
  - 執行 `python -m py_compile ...`、`python -m unittest discover -s tests -v`、`git diff --check`。

- [x] **Step 4 — 吞吐量 metrics（零 LLM）**（2026-07-20：attempt/completion JSONL + 1h/5h 任意視窗摘要；實跑記錄 timeout）
  - 每次呼叫追加結構化事件到 `logs/pipeline-metrics.jsonl`：slug、task、chunk、chars、開始／結束、秒數、success/timeout/quota、model、resume_hit。
  - 不把 prompt、原文或 token 寫進 metrics。
  - 新增唯讀摘要命令或現有狀態頁統計：最近 1h/5h 的成功 chunks、有效字元／小時、timeout 率、checkpoint 命中數、完成書數。
  - commit 數不得再當吞吐量指標。

- [x] **Step 5 — Checkpoint 小批驗收（不 push）**（2026-07-20：lamentations 真實續跑 1–8/8；多輪 quota 從缺段續接、最終原子發布、verify PASS）
  - 先選僅 1 個 FAILED chunk 的短檔（優先 `lamentations` 或 `sblgnt-galatians`）。
  - 人工製造／等待中段失敗後，確認下一輪從未完成 chunk 接續，不重跑已成功段。
  - 再測一部中篇與一部長篇；三部均通過 `verify.py --slug`，且 translation → tag 順序正確。
  - 驗收前保持 `--no-push`；確認 diff 只含指定 slug、metadata、狀態與索引。

- [x] **Step 6 — psych_tags 資料合約**（獨立欄位／狀態、48 個表格正式詞彙、舊 meta 向後相容）
  - `scripts/meta_template.json` 加 `"psych_tags": []` 與 `"psych_tag_status": null`，與既有 `semantic_tags`／`tag_status` 分欄。
  - whitelist 來源為 `00-overview/concepts-psychology.md` 的實際 45 個細群＋3 支流 slug；修正文內「46 細群」筆誤，不改詞彙本身。
  - 對接合約沿用 psychology-schools `00-overview/domains.json`：經文細群 slug → D1–D13 → `domain-index.json` → 學派。
  - religions-history 不重複儲存可推導的 D 碼，也不匯入 psychology-schools 的 36 個 `concept_tags`。

- [x] **Step 7 — 單次分類輸出三欄**（translate tag 與 classify-metadata 均同步；舊 semantic/keywords 在 psych backfill 時保留）
  - 擴充 tag prompt 與 JSON schema，一次回傳 `semantic_tags`、`psych_tags`、`keywords`。
  - semantic 與 psych 分別經自己的白名單過濾；keywords 維持自由詞 5–15 個。
  - 任一欄格式錯誤不得破壞既有 meta；先完整 parse/filter，再一次寫回。
  - 保留既有 `tag_status` 只代表 semantic tags；新增 `psych_tag_status` 只代表 psych tags。同一次分類成功時分別更新，不能把既有 semantic done 誤當 psych done。
  - 第一版維持現有全文 coverage，不同時引入摘要取樣最佳化。

- [ ] **Step 8 — 索引與 cross-project 驗收**（獨立索引與 4 部真實 psych 標記 PASS；20 部黃金樣本／跨專案 join 尚待擴充）
  - `scripts/build-tag-index.py` 增產 `00-overview/psych-tag-index.json`。
  - 從 `psych-tag-index.json` 抽至少 20 部核心經文，經 psychology-schools `domains.json` join 到 D1–D13，再連到 `domain-index.json`，不得有 orphan tag。
  - 黃金樣本優先用 `11-psychology/reference-analects.md`；人工核對 20 部，錯誤／過度標記率 >10% 即停止擴批、先改 prompt。

- [ ] **Step 9 — 分批恢復與後續最佳化判定**
  - 先重跑 30 部歷史 FAILED 翻譯，順序：短檔單一缺段 → 7 個未提交高失敗檔 → 其餘 tracked 部分缺段 → `quran` 最後獨立處理。
  - 每 5 部完成：重建索引、跑相關 slug verify；每個可提交小批前依 `CLAUDE.md` 跑 `verify.py --all`。
  - 收集至少 50 次成功 chunk 後再比較 3k／5k／7k 字元；只有有效字元／小時提升且 timeout 不顯著惡化才調整預設。
  - 是否改成摘要式單次 tag 另開 plan-check，不在本次順手加入。

## 驗收門檻

- [ ] 中段失敗後，下一輪第一個模型呼叫是缺失 chunk，不是 chunk 1。
- [ ] checkpoint 不完整時，正式翻譯、`translation_status`、`tag_status`、`psych_tag_status` 均不變。
- [ ] source／role／chunk 規則改變時，舊 checkpoint 不被混用。
- [ ] 30 部歷史 FAILED 翻譯全部「完整修復」或「明確留在 failed queue 且未標 done」。
- [ ] `verify.py --all` 不再出現 FAILED placeholder／假 done。
- [ ] `psych_tags` 100% 在 48 個正式表格白名單內；cross-project join orphan = 0。
- [ ] 同一部經分類只需一條 semantic+psych+keywords 流程，不新增第三輪全文分類。
- [ ] metrics 能回答最近一小時有效字元數、成功 chunks、timeout 率與完成書數。

## Commit 建議

1. `Fix pipeline completion and unattended resume guards`：整理目前工作樹中完成判定、verify、單實例與 watcher backoff 修復。
2. `Add resumable translation chunk checkpoints`：checkpoint + tests + metrics，不含資料重跑。
3. `Resume incomplete core translations with checkpoints`：短檔小批實測資料。
4. `Add psychology question tags and reverse index`：schema、雙白名單、三欄分類、索引與文件。

## 回滾

- Checkpoint 功能以單一路徑封裝；回滾時可停用 checkpoint、恢復整部重跑，正式翻譯格式不變。
- `psych_tags` 是新增欄位，回滾不需修改 `semantic_tags` 或原文。
- 若新分類品質未達門檻，只停止 psych 回填；既有 semantic tag pipeline 仍可獨立運作。

## 已完成

- [x] 2026-07-20：定位歷史假完成根因；盤點 30 部翻譯／733 個 FAILED chunks、2 部註釋／42 個 FAILED chunks。
- [x] 2026-07-20：目前工作樹已加入 FAILED-aware completion、翻譯後置檢查、tag guard、verify guard。
- [x] 2026-07-20：目前工作樹已加入 auto-pipeline／supervisor／quota watcher 單實例保護，以及 watcher 尊重 `next_retry_at`。
- [x] 2026-07-20：加入翻譯 checkpoint、metrics、心理標籤雙軸合約與獨立反向索引；13/13 測試通過。
