# 實作清單：核心 518 全量翻譯全綠 + psych 軸驗收 + 網站 v1 上線

> 來源：/plan-check（2026-07-21，本檔自足、可跨 session 接力）
> 工作目錄：`C:\claudehome\projects\religions-history`
> 接手須知：先讀 `CLAUDE.md` + `HANDOFF.md`；raw/ 與 checksums 永不觸碰；M3 同一時間只准一條生成管線。
> **派工規則**：主 session 若為 claude-fable-5 → 只規劃與派工（全域 CLAUDE.md #10），實作一律派 Agent / claude-m3 / minimax-m2.7；驗收讀檔與派工指令允許。
> 關聯文件：`.implementation_pipeline-checkpoint-psych-tags.md`（Step 8–9 由本檔 Step 4–7 承接）、`.implementation_site-v1-psych-tags.md`（由本檔 Step 8–9 承接）、`.implementation_pipeline-priority-retry.md` W25（由本檔 Step 1–2 承接）。

## 待執行

- [ ] **Step 5c**: chunk 污染防護 guard（源頭堵漏）：派 Sonnet agent 改 `scripts/translate.py`（或 chunk 接收處）加污染黑名單檢測 — chunk 輸出含 `已 commit` / `commit + push` / `PIPELINE_STATUS` / `HANDOFF` / `git push` / orchestrator 自述 pattern → 該 chunk 判 failed 走既有 retry，不落盤；並在 `scripts/verify.py` 加 `--contamination` 掃描模式（掃 `translations/*/01-translation.md` 報污染檔清單）。純本地程式碼，不打 M3 API，無錯峰問題。
  - 工具：Agent（model: sonnet, name: contamination-guard）
  - Skill：subagent-git-no-mutation
  - 派工：Sonnet
  - 派工理由：改管線核心邏輯需推理（不能誤殺正常經文含「手」「狀態」等字），fable-5 不動手
  - 風險：黑名單過寬誤殺正常翻譯 chunk → pattern 須錨定管線自述句式（如「已 commit」+ hash、「PIPELINE_STATUS.md」全名），驗收含 false-positive 抽測

- [ ] **Step 5d**: 59 份污染檔清理：guard 落地後，用 verify.py `--contamination` 輸出清單，逐檔清除污染行（污染是 M3 session 尾語成段附加，非混入經文行 → 可程式化切除；切除後該檔若缺 chunk 內容則重排進 failed queue 由管線重跑）。派 Sonnet 寫清理腳本 + 實跑 + 抽 5 檔人工核對。
  - 工具：Agent（sonnet）
  - Skill：subagent-git-no-mutation、verify-before-confirm
  - 派工：Sonnet
  - 派工理由：切除邊界判斷需語義理解；量大（59 檔）留子 agent context
  - 前置：Step 5c guard 先落地（否則管線邊跑邊產新污染，清不完）

- [ ] **Step 5e**: psych_tags 重測閘門：5b prompt 修正 + 5c/5d 落地後，重派 psych-audit（同 Step 5 協議，換 20 部樣本或原樣本重跑）驗錯標率 ≤10%。通過 → 決定既有 155 部是否整批重標（比較錯標分佈：若偏差集中在模板三 tag，可只重標含該三 tag 的部）；不通過 → 再改 prompt 迭代。
  - 工具：Agent（model: sonnet, name: psych-audit-2）
  - Skill：subagent-git-no-mutation
  - 派工：Sonnet
  - 派工理由：同 Step 5
  - 串行紀律：本步通過前，psych_tags 不擴批

- [ ] **Step 4**: Phase 1 管線自主跑至核心全量：順序＝26 份 FAILED 修復 → `mozi` 標籤 → 21 部 P0 → 其餘核心佇列 → `quran` 最後獨立。主 session 每次開始查 `PIPELINE_STATUS.md` + failed queue v2；supervisor 死或 blocked>0 → 派 claude-m3 executor（`tools/m3-executor-role.md`）重啟/重試；跨窗接力用 shotclock 單發（禁模型輪詢）。
  - 工具：Bash / claude-m3 -p（background）/ shotclock
  - Skill：subagent-git-no-mutation（每次派 m3 時）
  - 派工：claude-m3 管線（零 Anthropic 成本）
  - 派工理由：大量翻譯生成是 M3 月費甜蜜點；管線已具 checkpoint/重試/單實例鎖
  - 風險：plan-check 風險 1（M3 配額競爭）— 同一時間只准一條 M3 生成管線；Step 5/6 的 M3 批次啟動前先確認翻譯管線 idle 或錯峰

- [ ] **Step 5**: Phase 2a psych_tags 黃金樣本閘門（前置：庫內含 psych_tags 的部數 ≥20）：派 Sonnet agent 抽 20 部核心（優先 `11-psychology/reference-analects.md` 黃金種子）逐部核對 psych_tags 合理性，prompt 含 `00-overview/concepts-psychology.md` 白名單 + 反幻覺護欄（`tools/m3-reviewer-role.md`）+ 禁 git mutation，輸出核對紀錄檔 `logs/psych-golden-audit.md` + 錯標/過標率。>10% → 停批，改 `tools/m3-tagger-role.md` prompt 後重測。
  - 工具：Agent（model: sonnet, name: psych-audit）
  - Skill：subagent-git-no-mutation
  - 派工：Sonnet
  - 派工理由：語義判讀非機械比對，Haiku 易誤判；大量讀檔留在子 agent context
  - 風險：plan-check 風險 4（M3 幻覺污染心理學入口）；本步即品質閘門

- [ ] **Step 6**: Phase 2b 跨專案 join 驗證：產 `scripts/check-psych-join.py` 並實跑，orphan 必須 = 0。
  - 工具：Agent（sonnet：寫檔＋唯讀實跑）
  - Skill：subagent-git-no-mutation
  - 派工：**Agent(sonnet)**（改派紀錄：原標 minimax-m2.7＋Haiku；改派理由＝翻譯管線正連續打 M3 API，錯峰原則下不加 MiniMax 負載；m2.7 直打腳本屬 firn 專用 legacy）
  - 串行紀律：等 Step 5 psych-audit 回報驗收後才派
  - Spec 要點（已勘查資料形狀，07-21）：
    - 讀 `00-overview/psych-tag-index.json`（dict：tag → [{slug,name_zh,religion}]，現 42 tags）
    - 讀 psychology-schools `00-overview/domains.json`（`domains[]`：id D1–D13 × `rh_subtags` 45 unique 細群）→ 建 subtag→D 對照
    - 讀 psychology-schools `00-overview/domain-index.json`（dict D→學派 slug list，13 entries）
    - join 規則：index 中每 tag → ① 屬 3 支流（modern-tech-comparison / wisdom-learning-curiosity / nature-animals-ecology，domains.json _comment 明文不納入）＝預期不 join，單獨列示；② 在 subtag→D 對照 → D 必須存在於 domain-index 且學派 list 非空；③ 其餘＝orphan
    - boundary：掃 `translations/*/meta.json`，`psych_tags` 空陣列或 `psych_tag_status` null/非 done → 計入 skipped 不算 orphan；status=done 但 tag 不在 index＝index 過期告警
    - stdlib only、psychology-schools 唯讀、exit 0/1、`--ps-root` 可覆寫預設 `C:/claudehome/projects/psychology-schools`
  - 風險：4b 邊界（上列 boundary 已明定）

- [ ] **Step 7**: Phase 3 吞吐量判定（前置：≥50 次成功 chunk）：派 Haiku 跑 metrics 摘要命令彙整 `logs/pipeline-metrics.jsonl`（1h/5h 有效字元、成功 chunks、timeout 率、checkpoint 命中）→ fable-5 比較 3k/5k/7k 判定是否調 chunk 預設；只有「有效字元/小時提升且 timeout 不顯著惡化」才調，調整 diff 派工執行。
  - 工具：Agent（haiku）/ Bash
  - Skill：無
  - 派工：Haiku（彙整）＋自己做（判定＝規劃本體）
  - 派工理由：彙整 mechanical；判定需跨數據推理與規劃權責

- [ ] **Step 8**: Phase 4 前置整理：確認核心翻譯 ≥260/518 且 Step 5 閘門通過後，Edit `.implementation_site-v1-psych-tags.md` — 勾銷已被 checkpoint 清單做掉的重疊項（其 Step 2 三處同步＝checkpoint Step 6–7 已落地；其 Step 3 回填＝整合管線 translate→tag 三欄一次已取代），並把「自己做（Sonnet）」項改標「派 Agent(model: sonnet)」、Step 1「派 Opus」改標「fable-5 直接規劃」。
  - 工具：Edit
  - Skill：verify-before-confirm（勾銷前 Read 實際程式碼確認已落地）
  - 派工：自己做
  - 派工理由：文件協調需雙清單交叉脈絡

- [ ] **Step 9**: Phase 4 網站 v1：依調整後的 `.implementation_site-v1-psych-tags.md` 逐步執行（架構規劃 fable-5 自做 → minimax-m2.7 verbatim 寫檔 → Sonnet agent 設計實作與驗收 → Haiku curl 抽驗 → code-audit 收尾），該檔驗收全過（boundary 4 類、XSS escaped、alias 頁=0、Pages deploy success + curl 抽 5 頁 + Pagefind 實測命中）。
  - 工具：per 該檔（Agent / Bash / Edit / Skill）
  - Skill：frontend-design、subagent-git-no-mutation、code-audit、verify-before-confirm（per 該檔標註）
  - 派工：見該檔各步派工欄（fable-5 調整版）
  - 風險：Cloudflare Pages 20,000 檔/25MiB 上限 — 該檔 Step 7 是偵測點，未達標不設 CF

- [ ] **Step 10**: Phase 5 大宗收集決策：網站 v1 上線後，對 Pipeline A backlog（CBETA T18+ / Sefaria 深挖 / 教父全集 / 道藏 / 藏文）另跑 /plan-check 出獨立規劃。
  - 工具：Skill（plan-check）
  - Skill：plan-check
  - 派工：自己做（規劃本體）
  - 派工理由：規劃是 fable-5 權責

- [ ] **Step 11**: 收尾：README / HANDOFF 全面對齊（統計、網站連結、狀態快照）、四份舊 implementation 檔歸檔判定、最終 commit + push。
  - 工具：Edit / Bash
  - Skill：無
  - 派工：自己做
  - 派工理由：文件協調
  - 分散點：README ／ HANDOFF ／（PIPELINE_STATUS 自動生不手改）

## 進行中

- [ ] **Step 4**: Phase 1 管線自主跑至核心全量（claude-m3 管線）
  - 實況：07-21 15:40 恢復，155/518 起跑，sutta-nipata checkpoint 續跑中；跨 session 長程階段，每次 session 開始查 PIPELINE_STATUS + supervisor.pid

## 已完成

- [x] **Step 1**: HALT flag 歸因確認
  - 結果：flag = `manual maintenance: quota scheduling and deskboard freshness rollout`（07-21 06:37），對應維護清單均已完成，安全移除（15:38）
- [x] **Step 2**: 移除 HALT flag + 重啟 supervisor
  - 結果：`supervise-pipeline.py 核心` detached 啟動（supervisor PID 31344 / worker lock 31860）；`sutta-nipata` checkpoint resume 實證命中（resume 1/78 → chunk 2/78）（15:40）
- [x] **Step 3**: HANDOFF 更新
  - 結果：頂部快照改 07-21（規劃 pointer、恢復程序、untracked 檔處置、fable-5 規則）；舊快照降級為日期段（15:45）
- [x] **Step 5**: psych_tags 黃金樣本閘門（派 Sonnet psych-audit）
  - 結果：**FAIL** — 錯標率 11.0% > 10%，觸發停批。三個系統性偏差：① 泛用標籤套模板（death 60% / change-transformation 53% / calling-vocation 萬用）② 字面關鍵詞比對取代主題判斷（勇於敢則殺 ≠ courage，抽查證實）③ 讚頌/宇宙論誤讀為個人提問；另有漏標（Job 缺受苦意義類）、20 部全部恰 5 tags（模板證據）。紀錄：`logs/psych-golden-audit.md`。**附帶重大發現**：全庫掃描 59/288 份 `01-translation.md`（20.5%）含 M3 session 尾語污染（「已 commit + push」等）→ 增設 5c/5d 補救步（16:4x）
- [x] **Step 5b**: 修 `tools/m3-tagger-role.md` psych_tags 規則
  - 結果：新增「psych_tags 規則」專節四條硬規則（不強制標滿 5 / 文脈判斷非字面比對 / 讚頌宇宙論≠個人提問 / 核心主題必標不漏標）+ 判斷順序；逐條對應稽核三偏差＋漏標問題（fable-5 派工文件，允許自改）

## 驗收

- [ ] `PYTHONIOENCODING=utf-8 python scripts/verify.py --all` 全綠（零 FAILED placeholder / 假 done）
- [ ] `PIPELINE_STATUS.md` 顯示 518/518；failed queue v2 清空或全部明確 blocked
- [ ] 30 部歷史 FAILED 翻譯全部「完整修復」或「明確留在 failed queue 且未標 done」（checkpoint 清單門檻承接）
- [ ] `logs/psych-golden-audit.md` 存在、20 部核對紀錄、錯標率 ≤10%
- [ ] `check-psych-join.py` 輸出 orphan=0，且含空 psych_tags / null status 跳過的 boundary case 實測
- [ ] metrics 摘要能答：最近 1h 有效字元數、成功 chunks、timeout 率、完成書數
- [ ] 網站 v1：`.implementation_site-v1-psych-tags.md` 驗收區全勾（boundary / XSS / alias=0 / Pages deploy + curl 抽 5 頁 + Pagefind 命中）
- [ ] 每 phase 收尾：commit → push → HANDOFF/README 對齊 → GitHub commits 頁可見

## 建議補的 skill（執行中發現）

（執行時若遇到反覆出現但無對應 skill 的動作，記在這裡，wrap 時參考）
