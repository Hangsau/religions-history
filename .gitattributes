# Force LF line endings for all text files so SHA-256 checksums stay reproducible
# across Windows / macOS / Linux clones.
* text=auto eol=lf

# Explicit binary types (none currently, but reserved for future)
*.png binary
*.jpg binary
*.pdf binary
*.epub binary
