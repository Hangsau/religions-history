# 實作清單：religions-history 網站 v1 + 心理學標籤軸上線

> 來源：/plan-check（2026-07-06，本檔自足、可跨 session 接力）
> 工作目錄：C:\claudehome\projects\religions-history
> 接手須知：先讀本專案 `CLAUDE.md` + `HANDOFF.md`；所有 Python 跑 `PYTHONIOENCODING=utf-8`；raw/ 與 checksums 永不觸碰；meta.json 只加欄位不改語意。
> Fleet 計費：Opus/Sonnet/Haiku＝Claude 訂閱配額；claude-m3＝MiniMax 月費（零 Anthropic 成本）；minimax-m2.7＝MiniMax 月費 verbatim 發包；deepseek-v4-flash＝現金按量，只留 fallback。

## 待執行

- [ ] **Step 1 (W0)**: 派 Opus 做網站架構 plan-check 批次，一個視窗產出 `planning/site-v1-architecture.md`，內容必含：① 四軸 URL scheme（`/religion/<id>`、`/question/<domain>`（13 領域，kebab id 取自 `00-overview/concepts-psychology.md`）、`/concept/<tag>`（14 類白名單 `00-overview/concepts.md`）、`/text/<slug>`）② 大文本分章策略（>300KB 檔按 `=== N | label ===` 分頁的規則與檔數估算）③ Pagefind 索引分層（全文層＝核心+已譯 ~700 部；meta 層＝其餘；估算 dist 總檔數與大小 vs Cloudflare Pages 20,000 檔/25MiB 限制）④ 設計方向定稿（基準：老派印刷×學術期刊混搭、思源宋體 body、鐵則 G 空間化左欄兩層樹、首頁 13 領域引導入口；須逐條過 DESIGN_SYSTEM 鐵則 A–H）。prompt 明確禁止 git 操作、只准新增 planning/ 一檔。
  - 工具：Agent（subagent_type: Plan 或 general-purpose，model: opus）
  - Skill：subagent-git-no-mutation
  - 派工：Opus
  - 派工理由：plan-check 規定需 Opus；批量一個視窗規劃完，省配額
  - 風險：無（唯讀研究 + 單檔新增）

- [ ] **Step 2 (W1)**: psych_tags 三處同步（微 diff，不發包）：① `scripts/meta_template.json` 加 `"psych_tags": []`（緊鄰 `semantic_tags` 欄位後）② `tools/m3-tagger-role.md` 加「人問軸」章節：標 `psych_tags` 時只准用 `00-overview/concepts-psychology.md` 的 46 細群 kebab 標籤、允許多標籤、與 semantic_tags（教義軸）正交不混填 ③ orchestrator 白名單過濾：`Grep "semantic_tags" scripts/auto-pipeline.py scripts/translate.py` 定位既有 tag 過濾函式，比照新增 psych_tags 過濾（白名單來源改讀 concepts-psychology.md，違規 tag 丟棄 + log）。④ `scripts/build-tag-index.py` 比照 tag-index 增產 `00-overview/psych-tag-index.json`。完成即 commit。
  - 工具：Edit / Grep
  - Skill：無
  - 派工：自己做
  - 派工理由：微 diff（每處 ≤10 行）不發包；三處同步需 session 脈絡
  - 分散點：meta_template.json ／ m3-tagger-role.md ／ orchestrator 過濾器 ／ build-tag-index.py（四處缺一 = 幻覺 tag 洩漏或索引缺軸）
  - 風險：4b 外部輸入——M3 輸出必經白名單過濾才寫 meta

- [ ] **Step 3 (W1b)**: 核心 504 部 psych_tags 回填：以 `11-psychology/reference-analects.md` 20 部黃金種子當 few-shot 先驗，寫批次腳本（或擴充既有 tag 流程）逐部呼叫 claude-m3，輸出 JSON→過濾→回填 meta.json，每 20 部 commit + push。啟動前：讀 `00-overview/PIPELINE_STATUS.md` 取 current slug，處理清單排除管線本輪佇列交集；跳過 `alias_of` 部。啟動後立刻設 /loop 監控（不問用戶）。抽樣驗收：每批抽 20 部人工比對，違規率 >10% 停批改 prompt（比照 `tools/m3-reviewer-role.md` 反幻覺護欄）。
  - 工具：Bash（claude-m3 -p background）/ Write / Skill
  - Skill：subagent-git-no-mutation、async-task-start-loop-directly
  - 派工：claude-m3（執行）＋自己（腳本與驗收）
  - 派工理由：大量小輸出分類，月費零成本；驗收留主 session
  - 風險：風險 2 配額競爭（與翻譯管線錯峰）、風險 3 M3 幻覺、4b 並發（排除佇列交集，撞了以 git 復原）

- [ ] **Step 4 (W2)**: Layer 2 全庫 classify（era + genre + semantic_tags + keywords + psych_tags 單一呼叫/部，~4,000 部）：Sonnet 寫完整 spec（含 prompt 範本、白名單、JSON schema、續跑 --skip-done、失敗隊列、429 偵測寫 HALT-classify flag），發包 minimax-m2.7 產 `scripts/classify-batch.py`（1 call = 1 file），Sonnet diff 驗收後由 claude-m3 supervisor 式續跑。排程與 Step 3 及翻譯管線錯峰（夜間或中文 verbatim 段放行）。
  - 工具：Agent / Bash / Write
  - Skill：subagent-git-no-mutation、async-task-start-loop-directly
  - 派工：minimax-m2.7（寫檔）＋ claude-m3（跑批）＋自己（spec + 驗收）
  - 派工理由：完整新檔 150–300 行是 verbatim 發包甜蜜點；跑批零 Anthropic 成本
  - 風險：風險 2 配額競爭、4b 並發（同 Step 3 排他規則）、發包後測試失敗先 diff spec

- [ ] **Step 5 (W3)**: Astro scaffold（依 Step 1 架構文件）：Sonnet 逐檔寫 self-contained spec，發包 minimax-m2.7（1 call = 1 file）：`site/src/lib/loader.ts`（讀 `translations/**/meta.json`；跳過 alias_of；translation_status null＝未譯；text_role null 安全預設；空 raw 標記占位頁）、動態路由四軸頁、Pagefind 整合、`site/build.mjs`。**XSS 硬規則寫進每份 spec：經文內容一律 escaped 純文字渲染，禁 `set:html`**。每檔驗收後 commit。
  - 工具：Agent / Write / Bash
  - Skill：subagent-git-no-mutation
  - 派工：minimax-m2.7（寫檔）＋自己（spec、組裝、驗收）
  - 派工理由：多個 50–300 行新檔；組裝與邊界判斷需 session 脈絡
  - 風險：4b 邊界（空檔/超大檔/舊 meta/alias 四類）、4b XSS、風險 1（檔數在 Step 7 實測）

- [ ] **Step 6 (W4)**: 版面與設計實作：先重讀 `C:\claudehome\resources\notes\DESIGN_SYSTEM.md`，依 Step 1 定稿方向做 layout/CSS/字型/首頁；經文頁用「單一左欄兩層樹（全站導航上＋本頁 `=== N | ===` 章節目次下）＋內容右欄」；CJK body 思源宋體、行長 25–40 字、行高 ≥1.7；首頁＝13 領域引導卡片＋宗教次入口＋搜尋第三，不攤開全庫清單；`text_role=translation` 頁面標「本文為英譯，原文待補」。完成後鐵則 A–H 逐條過閘。
  - 工具：Edit / Write / Bash（dev server 實測）
  - Skill：frontend-design
  - 派工：自己做（Sonnet）
  - 派工理由：設計敏感 + 需比對 DESIGN_SYSTEM 與 Vortex 已驗證版型經驗，不外包
  - 風險：鐵則衝突時可用性優先於美學

- [ ] **Step 7 (W5)**: 本機 build 實測 → 部署：`npm run build` 後 `find dist -type f | wc -l` + `du -sh dist`；超過 18,000 檔或索引過大 → 依預案降級（全文索引只收核心+已譯，其餘 meta 索引；再超則大文本改單頁+章節懶載入）。達標後設 Cloudflare Pages 接 GitHub repo，push 觸發部署。**驗 deploy：Pages build 狀態 success + curl 線上抽 5 頁（四軸各一＋首頁）驗內容 + Pagefind 搜「無常」「creation」回真實命中**（push 成功 ≠ 上線）。
  - 工具：Bash / PowerShell / Agent（haiku 跑 curl 驗證）
  - Skill：verify-before-confirm
  - 派工：自己做（CF 設定）＋ Haiku（curl 抽驗）
  - 派工理由：外部服務設定不可逆性低但需判斷；抽驗是純 mechanical
  - 風險：風險 1 Pages 上限（此步是偵測點，未達標不設 CF）

- [ ] **Step 8 (W6)**: 收尾：/code-audit 全段程式碼審查 → 文件對齊（README 加網站連結與統計、HANDOFF 更新狀態快照、新建 MAP.md、CLAUDE.md 補 site/ 段落）→ 本檔加進 `.gitignore` → 最終 commit + push。
  - 工具：Skill / Edit / Bash
  - Skill：code-audit
  - 派工：自己做
  - 派工理由：審查與文件對齊需全程脈絡
  - 風險：大型改動 push 前 commit → code-audit → 文件對齊 → push 順序缺一不可

## 進行中

（執行時把當前步驟移到此區）

## 已完成

（完成後移到此區，附一行結果摘要 + 時間）

## 驗收

- [ ] Step 2/3：核心 meta 全數含 psych_tags 欄（`grep -L psych_tags` 為空）；過濾 log 違規率 <10%；抽 20 部人工核對相符
- [ ] Step 4：era/genre null 率 <5%（audit-core + 刊版對齊覆蓋率）
- [ ] Step 5 boundary test：空檔 slug／>1MB slug／無 translation_status 舊 meta／alias_of slug 四類餵 loader，行為符合定義；**48 部 alias 生成頁數 = 0**
- [ ] Step 5 XSS test：塞含 `<script>` 假原文 build，輸出 HTML 為 escaped 文字
- [ ] Step 6：DESIGN_SYSTEM 鐵則 A–H 逐條過閘清單 + CJK 行長行高實測截圖
- [ ] Step 7：CF Pages deploy success + curl 線上 5 頁內容正確 + Pagefind 兩詞真實命中
- [ ] Step 3/4 併發：classify 與 supervisor 同跑 30 分鐘後 `git log --follow` 抽查 10 個 meta 無覆蓋遺失

## 建議補的 skill（執行中發現）

（執行時若遇到反覆出現但無對應 skill 的動作，記在這裡，wrap 時參考）
