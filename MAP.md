# religions-history code map

## Generation execution path

`status_gui.py` / Windows Scheduled Task → `supervise-pipeline.py` → `auto-pipeline.py`
→ `translate.py` → MiniMax Anthropic-compatible endpoint.

- `scripts/translate.py`: chunk checkpoints, quota preflight, provider calls, metrics and runtime handoff.
- `scripts/minimax_quota.py`: read-only quota probe and configurable 10% reserve policy.
- `scripts/auto-pipeline.py`: priority queue, continuously rebuilt retry scheduling, scoped commits.
- `scripts/pipeline_failures.py`: ordinary scripture failures only; 5/15/30-minute retry then blocked.
- `scripts/quota-watch-resume.py`: quota/provider wait and checkpoint resume.
- `scripts/supervise-pipeline.py`: single long-lived supervisor, HALT and crash recovery.

## Reporting path

- `logs/pipeline-runtime.json`: authoritative live/wait/paused state and handoff point.
- `logs/pipeline-failed.json`: retryable/blocked scripture failures.
- `logs/pipeline-metrics.jsonl`: append-only attempt and completion metrics.
- `scripts/status_gui.py`: desktop board, operational controls and diagnostic export.
- `scripts/status.py`: generated `00-overview/STATUS.md` snapshot.
- `scripts/track-progress.py`: generated `PROGRESS.md/.json`; translations require both done metadata and a complete file.
- `00-overview/PIPELINE_STATUS.md`: queue summary generated by the orchestrator.

## Safety controls

- `logs/pipeline-HALT.flag`: safe pause at the next scripture boundary.
- `logs/auto-pipeline.lock`: prevents duplicate MiniMax generation.
- `logs/pipeline-checkpoints/<slug>/<task>/active/`: resume point for long translations.
- `scripts/install-pipeline-task.ps1`: optional Task Scheduler installer; it is not registered automatically.
